# Database
DATABASE_PATH=./data/readingcircle.db

# JWT Secrets (change these to random strings!)
JWT_SECRET=change-me-to-a-random-secret
JWT_REFRESH_SECRET=change-me-to-a-random-refresh-secret

# Initial Admin (required on first run)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# SMTP Configuration (optional - emails logged to console if not set)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=user@example.com
SMTP_PASS=password
SMTP_FROM=readingcircle@example.com

# App
APP_URL=http://localhost:5173
PORT=3000
